// Chaînes de caractères
// https://developer.mozilla.org/fr/docs/Web/JavaScript/Reference/Objets_globaux/String

// Guillemets simple, doubles ou accents grave ?
let a = 'Bonjour'; // simples (single quote): Méthode préférée pour le code
let b = "Bonjour"; // doubles (double quote): Aussi valable, requis pour JSON

// Littéraux de gabarits (template litterals)
let c = `Bonjour`; // accents graves (backticks)
// permet le multilignes, les injections d'expressions avec ${expression}
c = `Le résultat est ${x + y}`;
