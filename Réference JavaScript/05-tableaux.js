// Tableaux
// https://developer.mozilla.org/fr/docs/Web/JavaScript/Reference/Objets_globaux/Array

const a = [1, 2, 3, 4];
a[0];   // 1
a[3];   // 4

// Propriété length
a.length // 4

a.push(5)   // ajoute un élément à la fin du tableau et retourne sa nouvelle longueur
a.pop()     // retourne le dernier élément et le supprime du tableau
